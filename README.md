# recoveryS
recoveryS monitors Docker container health on Ubuntu Server for Raspberry Pi 5. It restarts Docker after persistent failures and requests one orderly reboot if recovery fails. Includes reboot-loop protection, configurable monitoring, timestamped logs, CLI controls, and optional hardware watchdog setup.
